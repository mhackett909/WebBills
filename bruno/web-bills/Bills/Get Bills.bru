meta {
  name: Get Bills
  type: http
  seq: 1
}

get {
  url: {{BILLS_URL}}/api/v1/bills?status=active
  body: none
  auth: bearer
}

params:query {
  status: active
}

auth:bearer {
  token: {{jwtToken}}
}
